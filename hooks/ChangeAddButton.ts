import { useSelector } from 'react-redux'; 

// export default function ChangeAddButton() {
//   const state = useSelector(state => state.CartReducer);

//   state.cartItems.find(item=>item.id==id) && state.cartItems.find(item=>item.id==id).qy > 0
// }

